[core]
excludesfile = ~/.gitignore_global

[user]
name = Niko Dziemba
email = niko@dziemba.com

[push]
default = simple

[alias]
aa = add -A
fs = !git fetch --all && git status
st = status
co = checkout
df = diff
ds = diff --staged
cm = commit
pr = pull --rebase
rc = rebase --continue
rs = rebase --skip

